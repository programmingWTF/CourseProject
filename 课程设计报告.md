# 程序设计课程设计报告

## 一、需求分析

### 项目背景

随着激光雷达、深度相机等三维感知设备在自动驾驶、机器人导航、工业测量等领域的广泛应用，点云数据已成为三维场景感知的核心数据形式。然而，原始采集的点云数据通常包含大量噪声点、离群点和冗余信息，直接使用会影响后续处理（如三维重建、目标检测、路径规划等）的精度和效率。因此，需要一套通用的点云预处理与特征提取流水线工具，帮助研究者或开发者快速完成以下工作流：

1. **加载多种格式的点云文件**（PCD、PLY、KITTI bin 等），统一为内部数据结构；
2. **组合多种滤波算法**，灵活去除噪声与冗余信息；
3. **提取几何特征**（法向量、主曲率等），为高层处理提供基础；
4. **交互式可视化**，直观对比处理前后效果，调试参数；
5. **批量保存中间结果**，形成可复用的处理流水线。

本系统面向三维感知方向的科研人员或工程师，为其提供一个开箱即用、界面友好的交互式点云处理台。

### 为什么采用 C++

1. **性能要求**：点云数据动辄数十万甚至数百万个三维点，滤波和特征提取涉及大量数值计算（K近邻搜索、协方差矩阵求解等），需要 C++ 的零开销抽象和编译期优化能力来保证实时交互体验。
2. **生态支持**：点云处理领域最权威的开源库 **PCL（Point Cloud Library）** 本身就是 C++ 头文件/模板库，与 C++ 项目天然无缝集成。
3. **课程要求**：程序设计课程设计旨在锻炼面向对象程序设计能力（封装、继承、多态、模板等），C++ 是本课程所要求的开发语言。
4. **跨平台扩展性**：借助 CMake 构建系统和 vcpkg 包管理，同一套 C++ 代码可方便地移植到 Linux / macOS 平台，满足未来部署需求。
5. **GUI 与 OpenGL 集成**：ImGui 是 C++ 生态中最轻量的即时模式 GUI 库，可直接嵌入 OpenGL 窗口，结合 PCL 内置的 PCLVisualizer 实现高性能 3D 渲染。

---

## 二、程序的主要功能

本程序为**自拟选题**——"点云处理流水线可视化控制台"，核心功能围绕「加载→滤波→特征提取→可视化」的完整处理流程展开。

### 1. 文件加载功能

支持加载多种主流点云文件格式：

- `.pcd`：PCL 原生格式，支持 ASCII 与二进制
- `.ply`：Stanford PLY 格式
- `.bin`：自动识别 KITTI（每点 16 字节，含 intensity）与纯 XYZ（每点 12 字节）两种变体

用户可通过以下两种方式加载点云文件：
- 在"搜索文件夹"路径框中输入数据目录路径，从文件浏览器面板中点击文件名选中，再点击"加载当前选择文件"按钮
- 点击"浏览..."按钮，通过 Windows 原生文件对话框浏览并选择文件

加载成功后，界面会显示文件名及总点数（如 `dragonStandRight_336.ply (437645 points)`）。

### 2. 滤波处理功能

提供三种 PCL 标准滤波算法，用户可自由组合、排序：

- **PassThrough（直通滤波）**：按指定坐标轴（x / y / z）裁剪点云范围，可拖拽调节最小/最大值，支持 [-500, 500] 大范围适应 KITTI 等大场景数据。如果最小值 ≥ 最大值，界面会给出红色警告提示。
- **VoxelGrid（体素网格滤波）**：对点云进行降采样，在保留整体几何形态的前提下大幅减少点数，提升后续处理速度。叶片大小（leaf size）支持 0.001 m 到 0.5 m 的细致调节。
- **StatisticalOutlier（统计离群点去除）**：基于邻域统计分析去除稀疏离群噪声点。可调节均值 K 值（1-200）和标准差乘数（0.1-5.0）。

### 3. 特征提取功能

- **法线估计（Normal Estimation）**：基于每个点的 K 近邻邻域拟合局部平面，计算法向量。支持 K 近邻搜索与半径搜索两种模式。
- **主曲率计算（Principal Curvature Estimation）**：在法线估计的基础上求解主曲率（pc1），为后续点云分割、特征点检测等高级任务提供几何特征。法线计算结果可在流水线中自动复用，避免重复计算。

### 4. 可视化功能

- **多视口 3D 渲染**：原始点云固定占据一个视口，流水线中每个"显示点云"或"显示法线"步骤各新增一个视口，所有视口水平并列排布，方便横向对比。
- **丰富的显示参数调节**：每个视口可独立设置点云颜色（RGB）、点大小、法线显示密度/长度、坐标轴显示/缩放/线宽、背景颜色等。
- **原始点云显示参数**：支持独立的颜色（默认白色）和坐标轴显示。

### 5. 流水线编辑器

- **步骤增删改排序**：支持添加 6 种步骤类型（3 种滤波 + 曲率计算 + 显示点云 + 显示法线），每个步骤在独立卡片中展示参数控件。步骤可通过"上"/"下"按钮拖拽排序，支持单个删除。
- **多列水平布局**：步骤卡片以多列网格排列（最多 5 列），充分利用宽屏空间。
- **参数实时调节**：所有滤波参数、特征提取参数、显示参数均通过滑块、下拉菜单、复选框等交互控件实时调节。

### 6. 异步执行与输出

- **后台异步执行**：点击"执行流水线"后，滤波与特征计算在后台线程异步运行，不阻塞 UI 操作（界面显示"⏳ 流水线正在后台执行中..."），执行完成后自动更新所有视口。
- **结果统计**：执行完成后显示精确的点数变化（原始点数 → 处理后点数，含绝对差值及百分比）。
- **步骤输出保存**：勾选各步骤的"保存输出"复选框后，执行时会在源文件所在目录的 `output/` 子文件夹下按步骤编号保存中间结果（如 `cloud_01.pcd`、`cloud_02.pcd` 等），便于离线调试和复现。
- **处理日志**：所有滤波操作的名称、处理前后点数、差值均记录到 `logs/processing_gui.log` 文件，包含时间戳，可追溯每次处理的历史记录。
- **重置功能**：点击"重置"按钮恢复到原始点云，流水线状态清空。

---

## 三、程序运行平台

- **操作系统**：Windows 11
- **开发环境**：Visual Studio 2022 + MSVC 编译器
- **构建工具**：CMake 3.10+
- **包管理器**：vcpkg（管理 PCL、GLFW 等第三方依赖）
- **C++ 标准**：C++17

### 编译步骤

1. 通过 vcpkg 安装依赖：
   ```
   vcpkg install pcl:x64-windows glfw3:x64-windows
   ```

2. 使用 CMake 配置并指定 vcpkg toolchain：
   ```bash
   cmake -B build -DCMAKE_TOOLCHAIN_FILE=D:/Software/vcpkg/scripts/buildsystems/vcpkg.cmake
   ```

3. 编译：
   ```bash
   cmake --build build --config Release
   ```

4. 运行：
   ```bash
   cd build/Release
   ./Point-Cloud-Pipeline-Control.exe
   ```

### 具体操作流程

1. 启动程序后，在"文件选择区"的"搜索文件夹"输入框中输入点云数据所在目录路径（默认为 `../data`）；
2. 下方文件浏览器面板会列出该目录下所有 `.pcd` / `.ply` / `.bin` 文件，点击文件名选中，再点击"加载当前选择文件"（也可点击"浏览..."按钮通过系统对话框选择）；
3. 加载成功后，左侧出现 PCL Viewer 3D 渲染窗口，右侧为流水线控制面板；
4. 在"流水线自定义区域"中，通过"上/下"按钮调整步骤顺序，在每张步骤卡片中调节滤波参数（如直通滤波的轴向和范围、体素大小、离群点检测的 K 值和标准差等）；
5. 点击"新增步骤类型"下拉菜单选择步骤类型，再点"+ 添加步骤"按钮添加新步骤；
6. 如需保存中间结果，勾选各步骤卡片中的"保存输出"复选框；
7. 点击"执行流水线"按钮，程序在后台异步执行滤波和特征提取；
8. 执行完成后，PCL Viewer 的多视口会更新显示每个步骤的结果；右侧面板显示点数变化统计；
9. 可随时修改参数并重新执行，或点击"重置"按钮恢复到原始点云。

---

## 四、系统总框架图

```
┌──────────────────────────────────────────────────────────────────────┐
│                          main.cpp (GUI 主程序)                        │
│  ┌─────────────────┐  ┌──────────────────┐  ┌─────────────────────┐  │
│  │  ImGui 控制面板   │  │  PCLVisualizer   │  │  流水线异步执行引擎   │  │
│  │  - 文件选择区     │  │  - 多视口渲染     │  │  - std::async启动    │  │
│  │  - 流水线编辑器   │  │  - 点/法线/坐标轴  │  │  - 独立线程运行     │  │
│  │  - 参数调节       │  │  - 背景/颜色       │  │  - 执行完回调更新UI  │  │
│  └─────────────────┘  └──────────────────┘  └─────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
          │                      │                       │
          ▼                      ▼                       ▼
┌──────────────────┐  ┌──────────────────┐  ┌────────────────────────┐
│  PointCloudIO    │  │ PointCloud       │  │     ProcessingLog      │
│  - load()        │  │ Pipeline         │  │  - log()               │
│  - save()        │  │ - addStage()     │  │  - 时间戳 + 点数记录    │
│  PCD/PLY/BIN读写  │  │ - execute()     │  └────────────────────────┘
└──────────────────┘  │ - setExtractor() │
                      └──────┬───────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │ FilterBase  │  │ FeatureBase │  │ FeatureBase │
    │  (虚基类)    │  │ <PointXYZ, │  │ <PointXYZ,  │
    │ - getName() │  │   Normal>   │  │   Curvature>│
    │ - apply()   │  │ - extract() │  │ - extract() │
    └──────┬──────┘  └──────┬──────┘  └──────┬──────┘
           │                │                 │
    ┌──────┼──────┐         ▼                 ▼
    ▼      ▼      ▼   NormalExtractor  CurvatureExtractor
 PassThrough  VoxelGrid  StatisticalOutlier
```

### 数据流

```
[文件系统]  →  PointCloudIO::load()  →  pcl::PointCloud<PointXYZ>::Ptr
                                           │
                                ┌──────────┴──────────┐
                                ▼                      ▼
                      [ImGui 控制面板显示]    [PCLVisualizer 原始视口]
                                │
                                ▼
                      [流水线编辑器：用户配置步骤]
                                │
                                ▼
               ┌── [直通滤波] → [体素滤波] → [统计滤波] ──┐
               │                                          │
               ▼                                          ▼
        pcl::PointCloud<PointXYZ>::Ptr              [法线估计]
               │                                          │
               └────────────────→ [曲率计算] ←────────────┘
                                         │
                                         ▼
                              [多视口 3D 可视化]
                              [output/ 中间结果保存]
                              [logs/ 处理日志记录]
```

---

## 五、程序类的说明

### 1. FilterBase（滤波器抽象基类）

- **文件**：`include/FilterBase.hpp`
- **作用**：定义滤波器的统一接口，所有滤波算法必须继承此类。
- **纯虚函数**：
  - `getName()`：返回滤波器名称，用于日志记录
  - `apply(cloud)`：对传入的点云智能指针执行滤波处理，直接修改原指针

### 2. PassThroughFilter（直通滤波器）

- **文件**：`include/Filters.hpp`、`src/Filters.cpp`
- **继承**：`FilterBase`
- **成员变量**：`field_name_`（滤波轴）、`limit_min_` / `limit_max_`（范围）
- **实现**：内部使用 `pcl::PassThrough<pcl::PointXYZ>`，按指定坐标轴裁剪点云

### 3. VoxelGridFilter（体素网格滤波器）

- **文件**：`include/Filters.hpp`、`src/Filters.cpp`
- **继承**：`FilterBase`
- **成员变量**：`leaf_size_`（体素叶片大小）
- **实现**：内部使用 `pcl::VoxelGrid<pcl::PointXYZ>`，对点云进行降采样

### 4. StatisticalOutlierFilter（统计离群点滤波器）

- **文件**：`include/Filters.hpp`、`src/Filters.cpp`
- **继承**：`FilterBase`
- **成员变量**：`mean_k_`（邻域点数量）、`std_dev_mul_thresh_`（标准差乘数阈值）
- **实现**：内部使用 `pcl::StatisticalOutlierRemoval<pcl::PointXYZ>`，基于邻域统计去除离群点

### 5. FeatureBase<PointInT, FeatureOutT>（特征提取模板基类）

- **文件**：`include/FeatureBase.hpp`
- **作用**：定义特征提取器的统一模板接口。模板参数 `PointInT` 为输入点类型，`FeatureOutT` 为输出特征类型。
- **纯虚函数**：`extract(input)`：从输入点云中提取特征，返回特征点云

### 6. NormalExtractor（法线提取器）

- **文件**：`include/FeatureExtractors.hpp`、`src/FeatureExtractors.cpp`
- **继承**：`FeatureBase<pcl::PointXYZ, pcl::Normal>`
- **成员变量**：`k_neighbors_`、`search_radius_`
- **实现**：内部使用 `pcl::NormalEstimation` + `pcl::search::KdTree`，支持 K 近邻和半径搜索两种模式

### 7. CurvatureExtractor（主曲率提取器）

- **文件**：`include/FeatureExtractors.hpp`、`src/FeatureExtractors.cpp`
- **继承**：`FeatureBase<pcl::PointXYZ, pcl::PrincipalCurvatures>`
- **成员变量**：`k_neighbors_`、`search_radius_`、`input_normals_`
- **特色**：支持外部传入法线云（`setInputNormals()`），复用已计算的法线避免重复计算

### 8. PointCloudPipeline（点云处理流水线）

- **文件**：`include/Pipeline.hpp`、`src/Pipeline.cpp`
- **作用**：管理滤波器阶段列表和特征提取器，按顺序执行处理流程。
- **关键方法**：
  - `addStage(filter)`：添加滤波器到流水线
  - `setNormalExtractor(ne)` / `setCurvatureExtractor(ce)`：设置特征提取器
  - `execute(cloud)`：依次执行所有滤波阶段和特征提取
  - `getNormals()` / `getCurvatures()`：获取提取结果

### 9. PointCloudIO（点云文件读写）

- **文件**：`include/PointCloudIO.hpp`、`src/PointCloudIO.cpp`
- **作用**：封装 PCL 文件 IO，支持 PCD、PLY、KITTI bin、XYZ bin 四种格式的读写。
- **关键方法**：
  - `load(filename)`：自动识别文件格式，返回点云智能指针
  - `save(filename, cloud)`：模板函数，支持 `PointXYZ` 和 `PointNormal` 的保存

### 10. ProcessingLog（处理日志）

- **文件**：`include/Logger.hpp`、`src/Logger.cpp`
- **作用**：以追加模式写入处理日志，记录每次滤波/特征提取的操作名称、处理前后点数及差值，附时间戳。

---

## 六、模块分析

### 模块一：文件加载模块

- **实现文件**：`PointCloudIO.cpp`
- **功能**：解析不同格式的点云文件，统一返回 `pcl::PointCloud<pcl::PointXYZ>::Ptr`
- **关键逻辑**：通过文件扩展名分支，PCD/PLY 使用 PCL 内置 IO，`.bin` 则根据文件大小自动判断 KITTI（16 字节/点）或纯 XYZ（12 字节/点）格式，使用二进制流读取并解析

### 模块二：滤波模块

- **实现文件**：`Filters.cpp`、`FilterBase.hpp`
- **功能**：提供三种滤波算法，通过 `FilterBase` 接口统一调用
- **关键设计**：使用模板辅助函数 `apply_pcl_filter()` 抽取 PCL 滤波器的公共模式（setInputCloud → filter → 替换指针），消除重复代码

### 模块三：特征提取模块

- **实现文件**：`FeatureExtractors.cpp`、`FeatureBase.hpp`
- **功能**：法线估计与主曲率计算
- **关键设计**：`CurvatureExtractor` 支持法线复用（`setInputNormals()`），当流水线中已计算法线时，曲率提取器直接使用已有法线而非重新计算，节省计算资源

### 模块四：流水线执行模块

- **实现文件**：`Pipeline.cpp`、`main.cpp` 中的 `execute_pipeline_steps()` 函数
- **功能**：根据用户配置的步骤列表，按顺序执行滤波、特征提取、视口快照和文件保存
- **关键设计**：
  - 滤波步骤可批量累积后在遇到"显示步骤"时统一执行，减少不必要的数据拷贝
  - 异步执行（`std::async`）不阻塞 ImGui 渲染循环，利用 `VisualizerData` + `std::mutex` 进行线程间数据传递

### 模块五：可视化模块

- **实现文件**：`main.cpp` 中的 `visualizer_thread_func()`、`build_viewer_scene()`
- **功能**：基于 PCLVisualizer 的多视口 3D 渲染
- **关键设计**：可视化线程独立于主线程运行，通过 `VisualizerData::should_update` 标志位和互斥锁实现数据同步；视口水平等分布局，每个视口独立配置颜色、点大小、法线显示等参数

### 模块六：GUI 交互模块

- **实现文件**：`main.cpp` 主循环部分
- **功能**：ImGui 控制面板，提供文件浏览、参数调节、流水线编辑等交互
- **关键设计**：
  - 中文 ImGui 界面（加载微软雅黑字体，支持 `ImWchar` 中文全字集）
  - 多列网格布局展示流水线步骤卡片
  - Windows 原生文件对话框（`GetOpenFileNameA`）提供熟悉的浏览体验
  - 关闭确认对话框防止误操作

### 模块七：日志模块

- **实现文件**：`Logger.cpp`
- **功能**：记录每次处理的操作名称、点数变化、时间戳
- **关键设计**：RAII 模式（构造函数打开文件、析构函数关闭文件），日志立即 flush 防止进程异常退出时数据丢失

---

## 七、比较有特色的函数

### 1. `execute_pipeline_steps()` — 流水线执行引擎

此函数是整个系统的核心调度逻辑，位于 `main.cpp` 中。特色在于：

- **智能批量执行**：滤波步骤在遇到"显示步骤"（ShowCloud / ShowNormals）之前累加缓存，遇到显示步骤时统一 `flush` 执行，既保证了显示视口的实时性，又避免了不必要的中间点云拷贝。
- **法线复用优化**：当流水线中既有法线显示又有曲率计算时，法线计算结果通过 `setInputNormals()` 传递给曲率提取器，避免重复的法线估计计算。
- **输出自动命名**：按步骤编号生成 `cloud_01.pcd`、`cloud_02.pcd` 等有序输出文件，带有 `PointNormal` 特征的智能合并（`build_featured_point_cloud()`）。
- **空流水线后备**：当流水线没有显示步骤时，自动生成一个包含最终结果的默认视口，确保用户始终能看到处理后结果。

### 2. `apply_pcl_filter<T>()` — 滤波公共模板

位于 `Filters.cpp` 的匿名命名空间中。这是一个模板辅助函数，通过 C++ 模板将三种 PCL 滤波器的"setInputCloud → filter → 替换指针"公共模式抽象为一行调用：

```cpp
template <typename PclFilter>
void apply_pcl_filter(PclFilter& pcl_filter, pcl::PointCloud<pcl::PointXYZ>::Ptr& cloud);
```

它消除了 `PassThroughFilter::apply()`、`VoxelGridFilter::apply()`、`StatisticalOutlierFilter::apply()` 中的重复代码，体现了"Don't Repeat Yourself (DRY)" 的工程原则。

### 3. `PointCloudIO::save<T>()` — 模板化点云保存

这是一个模板成员函数，支持 `pcl::PointXYZ` 和 `pcl::PointNormal` 两种点类型的保存。通过显式实例化（explicit template instantiation）避免了将模板实现放在头文件中的要求，实现了接口声明与实现分离的良好工程结构。

### 4. `PointCloudIO::load()` — 智能格式检测

对于 `.bin` 文件，函数通过文件大小自动判断格式类型——能被 16 整除但不能被 12 整除的按 KITTI（每点 4 个 float）解析，其余按纯 XYZ（每点 3 个 float）解析。这种方式无需用户手动指定格式，提升了易用性。

### 5. `visualizer_thread_func()` — 独立可视化线程

PCLVisualizer 的 `spinOnce()` 必须在独立线程中循环调用以保持渲染流畅。本函数通过 `std::atomic<bool>` 控制线程生命周期，通过 `VisualizerData` + `std::mutex` 接收主线程的数据更新（`should_update` 标志位），实现了安全的多线程可视化架构。

### 6. `setup_imgui_font()` — 中文字体自适应加载

按优先级依次尝试加载微软雅黑字体文件（项目内置 → 系统字体目录），失败时回退到 ImGui 默认英文字体 + 缩放补偿，确保在不同环境下 GUI 文字都能正常显示。

---

## 八、存在的不足与对策、编程体会

### 存在的不足

1. **平台局限性**：当前仅支持 Windows 平台（依赖 ImGui + GLFW + Win32 原生文件对话框、PCLVisualizer 的 Win32 窗口）。虽然核心逻辑（滤波、特征提取、IO）可跨平台，但 GUI 层需要适配（macOS Metro 风格文件对话框、Linux 下的 GTK/Qt 嵌入等）。

2. **格式支持有限**：`.bin` 格式仅支持 KITTI（每点 4 float）和纯 XYZ（每点 3 float）两种最常用的变体，不支持其他变体（如带法线的 6 元素格式、带 RGB 的格式等）。对策：可增加文件头检测或让用户手动选择格式。

3. **可视化性能**：当点云规模达到数百万点时，PCLVisualizer 的多视口渲染可能出现卡顿。对策：可增加视口级别的 LOD（Level of Detail）降采样显示，仅在导出时使用全量数据。

4. **流水线可恢复性受限**：当前流水线配置仅在内存中，关闭程序后不保存。对策：可增加流水线配置的 JSON 序列化/反序列化功能，支持"保存流水线"和"加载流水线"。

5. **缺少批处理模式**：只能交互式处理单个文件，无法对目录下所有文件批量执行同一条流水线。对策：可增加命令行模式或批量文件队列。

6. **曲率值可视化不足**：曲率计算结果（`PrincipalCurvatures.pc1`）作为 `curvature` 字段存储在 `PointNormal` 中，支持保存到文件，但可视化窗口中没有基于曲率值的伪彩色映射，用户体验不够直观。对策：未来可利用 PCLVisualizer 的 `addPointCloud` 颜色重载，按曲率值进行红-蓝伪彩色渲染。

### 编程体会

1. **面向对象设计的层次感**：本项目通过「抽象基类（FilterBase / FeatureBase）→ 具体实现（PassThroughFilter / NormalExtractor）→ 流水线组合器（PointCloudPipeline）」三层架构，清晰地将「接口定义」「具体算法」「业务流程编排」分离。这种分层使得新增滤波算法或特征提取器只需继承基类并实现纯虚函数，无需修改流水线引擎代码，充分体现了面向对象的**开闭原则（Open-Closed Principle）**。

2. **模板在科学计算中的威力**：`FeatureBase<PointInT, FeatureOutT>` 的模板设计使得同一套特征提取接口可以适配不同的输入点类型和输出特征类型，编译期类型检查保证了类型安全的同时避免了运行时开销。`apply_pcl_filter<T>()` 模板函数则展示了如何用少量泛型代码消除大量重复。

3. **多线程 GUI 编程的挑战**：异步流水线执行和独立可视化线程的设计是本次课程设计中最具挑战性的部分。通过 `std::atomic<bool>` + `std::mutex` + `VisualizerData` 的组合，实现了安全的数据共享和同步。体会最深的是：在多线程环境下，「数据所有权」的概念比「同步原语」更重要——明确哪个线程拥有数据、哪个线程只读、何时移交所有权，远比加锁复杂。

4. **C++ 生态工具的成熟度**：vcpkg 包管理器使 PCL 等大型库的依赖管理变得极其简单；ImGui 的即时模式 GUI 范式让交互式科研工具的界面开发效率大幅提升；CMake 的跨平台构建描述让项目结构清晰可维护。现代 C++ 生态的成熟度使得个人开发者也能构建出功能丰富的桌面应用。

5. **从"能跑"到"好用"的距离**：课程设计初期只需完成功能演示，但在实际迭代中，大量时间花在了：错误提示的用户友好性（如直通滤波范围不合法时的红色警告）、意外操作的防护（关闭确认对话框）、结果的用户可感知度（点数变化统计）等细节上。这让我认识到：**一个"好用的软件"和"能跑的代码"之间的差距，往往就在于这些看似微小的用户体验细节。**

6. **课程设计与实际工程的接轨**：本项目从需求分析到架构设计、从接口定义到异常处理、从多线程同步到日志记录，涵盖了完整软件工程生命周期的多个环节。通过这个项目，我不仅巩固了 C++ 面向对象编程知识，也对"如何用工程化的方式组织一个有一定复杂度的 C++ 项目"有了更深的理解——包括 CMake 构建系统、第三方依赖管理、代码风格统一（.clang-format、.clang-tidy）、Git 版本控制等实际开发中不可或缺的技能。
